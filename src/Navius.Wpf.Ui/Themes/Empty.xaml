<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:empty="clr-namespace:Navius.Wpf.Ui.Empty">

    <Style TargetType="{x:Type empty:NaviusEmpty}">
        <Setter Property="Foreground" Value="{DynamicResource Navius.Foreground}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type empty:NaviusEmpty}">
                    <Grid>
                        <!-- A dashed border needs a stroked shape rather than Border.BorderThickness,
                             which has no dash style; the 8px radius mirrors Navius.Radius.Card. -->
                        <Rectangle
                            Stroke="{DynamicResource Navius.Border}"
                            StrokeThickness="1"
                            StrokeDashArray="3 3"
                            RadiusX="8"
                            RadiusY="8" />
                        <ContentPresenter
                            Margin="32"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type empty:NaviusEmptyMedia}">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type empty:NaviusEmptyMedia}">
                    <Border x:Name="Root" Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Variant" Value="Icon">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Navius.Muted}" />
                            <Setter TargetName="Root" Property="Width" Value="40" />
                            <Setter TargetName="Root" Property="Height" Value="40" />
                            <Setter TargetName="Root" Property="CornerRadius" Value="{DynamicResource Navius.Radius.Control}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type empty:NaviusEmptyTitle}">
        <Setter Property="Foreground" Value="{DynamicResource Navius.Foreground}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type empty:NaviusEmptyTitle}">
                    <ContentPresenter
                        HorizontalAlignment="Center"
                        TextElement.Foreground="{TemplateBinding Foreground}"
                        TextElement.FontSize="{TemplateBinding FontSize}"
                        TextElement.FontWeight="{TemplateBinding FontWeight}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type empty:NaviusEmptyDescription}">
        <Setter Property="Foreground" Value="{DynamicResource Navius.MutedForeground}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type empty:NaviusEmptyDescription}">
                    <ContentPresenter
                        HorizontalAlignment="Center"
                        TextElement.Foreground="{TemplateBinding Foreground}"
                        TextElement.FontSize="{TemplateBinding FontSize}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
