using System.Windows.Controls;

namespace Navius.Wpf.Gallery.Pages;

/// <summary>Demonstrates NaviusAvatar (idle/error/delayed fallback states) and NaviusAccessibleIcon.</summary>
public partial class AvatarPage : UserControl
{
    public AvatarPage()
    {
        InitializeComponent();
    }
}
