<UserControl x:Class="Navius.Wpf.Gallery.Pages.ToggleGroupPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:togglegroup="clr-namespace:Navius.Wpf.Primitives.Controls.ToggleGroup;assembly=Navius.Wpf.Primitives"
             Background="{DynamicResource Navius.Background}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Self-contained: merges its own family theme directly, since Generic.xaml
                     is owned by a concurrent wave this cycle and is not touched here. -->
                <ResourceDictionary Source="pack://application:,,,/Navius.Wpf.Primitives;component/Themes/ToggleGroup.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Margin="24">
        <TextBlock Text="ToggleGroup" FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource Navius.Foreground}" Margin="0,0,0,12" />

        <TextBlock Text="Single selection (default; radio-like, click again to clear)" Foreground="{DynamicResource Navius.MutedForeground}" Margin="0,0,0,4" />
        <togglegroup:NaviusToggleGroup HorizontalAlignment="Left" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <togglegroup:NaviusToggleGroupItem Value="left" Content="Left" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="center" Content="Center" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="right" Content="Right" />
            </StackPanel>
        </togglegroup:NaviusToggleGroup>

        <TextBlock Text="Multiple selection" Foreground="{DynamicResource Navius.MutedForeground}" Margin="0,0,0,4" />
        <togglegroup:NaviusToggleGroup Multiple="True" HorizontalAlignment="Left" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <togglegroup:NaviusToggleGroupItem Value="bold" Content="B" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="italic" Content="I" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="underline" Content="U" />
            </StackPanel>
        </togglegroup:NaviusToggleGroup>

        <TextBlock Text="RovingFocus=false (every enabled item independently tabbable)" Foreground="{DynamicResource Navius.MutedForeground}" Margin="0,0,0,4" />
        <togglegroup:NaviusToggleGroup RovingFocus="False" HorizontalAlignment="Left" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <togglegroup:NaviusToggleGroupItem Value="one" Content="One" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="two" Content="Two" />
            </StackPanel>
        </togglegroup:NaviusToggleGroup>

        <TextBlock Text="Disabled" Foreground="{DynamicResource Navius.MutedForeground}" Margin="0,0,0,4" />
        <togglegroup:NaviusToggleGroup IsEnabled="False" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <togglegroup:NaviusToggleGroupItem Value="a" Content="A" Margin="0,0,4,0" />
                <togglegroup:NaviusToggleGroupItem Value="b" Content="B" />
            </StackPanel>
        </togglegroup:NaviusToggleGroup>
    </StackPanel>
</UserControl>
