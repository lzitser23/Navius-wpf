<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Table: keyed styles re-templating the native ListView + GridView into the Navius brand
        (no custom control classes, since WPF's own ListView/GridView already model a data table).

        Usage:
            <ListView Style="{StaticResource Navius.Table}"
                      ItemContainerStyle="{StaticResource Navius.Table.Row}"
                      ItemsSource="{Binding Rows}">
                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource Navius.Table.ColumnHeader}">
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" />
                    </GridView>
                </ListView.View>
            </ListView>

        Navius.Table only sets colors/chrome on the native ListView (its scrolling/virtualization
        machinery is left alone, not rebuilt); the header and row look are fully re-templated via
        Navius.Table.ColumnHeader and Navius.Table.Row, which is where the brand actually shows.
    -->

    <Style x:Key="Navius.Table" TargetType="ListView">
        <Setter Property="Background" Value="{DynamicResource Navius.Card}" />
        <Setter Property="Foreground" Value="{DynamicResource Navius.CardForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Navius.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style x:Key="Navius.Table.ColumnHeader" TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{DynamicResource Navius.Card}" />
        <Setter Property="Foreground" Value="{DynamicResource Navius.MutedForeground}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{DynamicResource Navius.Border}"
                        BorderThickness="0,0,0,1"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="Center"
                            TextElement.Foreground="{TemplateBinding Foreground}"
                            TextElement.FontSize="{TemplateBinding FontSize}"
                            TextElement.FontWeight="{TemplateBinding FontWeight}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Navius.Table.Row" TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Navius.CardForeground}" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{DynamicResource Navius.Border}"
                        BorderThickness="0,0,0,1"
                        Padding="{TemplateBinding Padding}">
                        <!-- GridViewRowPresenter is the canonical way to render a row's cells laid
                             out to the hosting GridView's own column widths. -->
                        <GridViewRowPresenter Columns="{TemplateBinding GridView.ColumnCollection}" Content="{TemplateBinding Content}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Navius.Muted}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Navius.Accent}" />
                            <Setter Property="Foreground" Value="{DynamicResource Navius.AccentForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
