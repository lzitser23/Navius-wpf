using System.Windows.Controls;

namespace Navius.Wpf.Gallery.Pages;

/// <summary>
/// Demonstrates NaviusScrollArea's overlay scrollbars: vertical-only overflow, both-axes
/// overflow (exercises the Corner part), and a custom ScrollHideDelay.
/// </summary>
public partial class ScrollAreaPage : UserControl
{
    public ScrollAreaPage()
    {
        InitializeComponent();
    }
}
