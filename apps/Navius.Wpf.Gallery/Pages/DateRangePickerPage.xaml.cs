using System.Windows.Controls;

namespace Navius.Wpf.Gallery.Pages;

/// <summary>
/// Interaction logic for DateRangePickerPage.xaml. The demo instance is declared in markup; the
/// AutomationId ("DateRangePickerDemo") is set there on the real control so an external FlaUI
/// harness can look it up.
/// </summary>
public partial class DateRangePickerPage : UserControl
{
    public DateRangePickerPage()
    {
        InitializeComponent();
    }
}
